# Posit Public Package Manager serves precompiled binaries for macOS, Windows, and
# common Linux distributions, so `renv::restore()` does not need a compiler.
# renv rewrites this URL to the matching Linux binary path automatically.
options(repos = c(CRAN = "https://packagemanager.posit.co/cran/latest"))

# source("renv/activate.R")
